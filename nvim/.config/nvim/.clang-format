# .clang-format
---
BasedOnStyle: LLVM # Start with LLVM's base style as it provides good defaults.

IndentWidth: 4 # Use 4 spaces for indentation.

BreakBeforeBraces: Attach # Keep opening braces on the same line as the control statement or function.

PointerAlignment: Left # Align pointers to the left (e.g., int* ptr).
DerivePointerAlignment: false # Use the explicitly set PointerAlignment style.

SpaceBeforeParens: ControlStatements # Add a space before parentheses in control statements (e.g., if (x)).

SortIncludes: true # Do not reorder includes; maintain the original order.
